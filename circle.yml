machine:
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - go get -t -d -v ./...
    - make build

test:
  override:
    - test `gofmt -l $(git ls-files | grep -e '\.go$' | grep -v -e vendor) | wc -l` = 0
